import {extend} from './utils';
import {base} from './base';

export default extend(base, {
    primary: '#E5E9F0',
    secondary: '#ECEFF4',
    primaryBackground: '#2E3440',
    negative: '#4C566A',
    positive: '#A3BE8C',
});